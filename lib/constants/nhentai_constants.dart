class NHentaiConstants {
  /* 
    static class members can be used without a class instance, as such:
    `AppwriteConstants.databaseId`

    non-static members needs an instance, as such:
    `AppwriteConstants inst = AppwriteConstants()`
    `inst.whatever`
  */

  /// This is the appwrite database ID
//   static const String databaseId = "642c7a1253042d094e41";
}
